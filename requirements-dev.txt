# DEVELOPMENT dependencies. Everything needed to reproduce the analysis,
# retrain the model and run the tests — but NOT needed to serve the app.
#
#     pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Exploration and charts (Phase 5)
matplotlib>=3.7
seaborn>=0.12
jupyterlab>=4.0
ipykernel>=6.25

# Model comparison only. Phase 18 chose HistGradientBoosting, which ships
# inside scikit-learn, so the deployed app does not need this.
xgboost>=2.0

# Tests (Phase 22)
pytest>=7.4
