# RUNTIME dependencies — what the deployed Streamlit app actually needs.
#
# Deliberately short. Streamlit Cloud installs this list on every cold start,
# so every package here is startup time. matplotlib, seaborn, jupyter,
# xgboost and pytest are all development-only and live in
# requirements-dev.txt instead.
#
# Before deploying (Phase 26), replace these lower bounds with exact pins:
#     pip freeze > requirements.txt
# then delete the lines you do not need. Exact pins are what make the
# deployment reproducible.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
streamlit>=1.30
