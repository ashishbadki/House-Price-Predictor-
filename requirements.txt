# House Price Predictor — dependencies
#
# These are LOWER BOUNDS, not exact pins. That is deliberate for now:
# pinning exact versions before we know the project works can cause
# install failures that have nothing to do with our code.
#
# In Phase 24, once everything runs, we will replace this file with an
# exact snapshot using:   pip freeze > requirements.txt
# That is what makes the project reproducible for anyone who clones it.

# --- Core data handling ---
pandas>=2.0
numpy>=1.24

# --- Visualisation (Phase 5: EDA) ---
matplotlib>=3.7
seaborn>=0.12

# --- Machine learning (Phases 10-19) ---
scikit-learn>=1.3
joblib>=1.3

# --- Notebooks (Phases 3-5) ---
jupyterlab>=4.0
ipykernel>=6.25

# --- Web application (Phase 21) ---
streamlit>=1.30

# --- Testing (Phase 22) ---
pytest>=7.4
